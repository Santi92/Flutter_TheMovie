name: test_themoviedb
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
#Network
  dio: ^4.0.0
  connectivity: ^3.0.3
  
#state management
  flutter_bloc: ^7.0.0
  bloc: ^7.0.0

  dartz: ^0.9.2
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
 